<form method="post" action="{{ path('app_historique_rapport_delete', {'id': historique_rapport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ historique_rapport.id) }}">
    <button class="btn">Delete</button>
</form>
